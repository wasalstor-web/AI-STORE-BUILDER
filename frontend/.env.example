# Frontend Environment Variables
# Copy to .env.local for local dev or set in Cloudflare Pages dashboard

# API URL — leave empty for local dev (uses Vite proxy)
# Set to your backend URL for production (e.g., https://api.yourstore.com/api/v1)
VITE_API_URL=
