# ═══════════════════════════════════════════════════════════
#  AI Store Builder — Environment Variables
#  Copy this to .env and update the values
#  Generate secrets: python scripts/generate_secrets.py
# ═══════════════════════════════════════════════════════════

# ── App ──
APP_NAME=AI-Store-Builder
APP_VERSION=2.0.0
APP_ENV=development        # development | production
DEBUG=true

# ── API ──
API_V1_PREFIX=/api/v1

# ── Database ──
# Development (SQLite — no setup needed):
DATABASE_URL=sqlite+aiosqlite:///./ai_store_builder.db
# Production (PostgreSQL):
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# ── Redis (required for background jobs & rate limiting in production) ──
REDIS_URL=redis://redis:6379/0

# ── JWT Auth (generate with: openssl rand -hex 64) ──
JWT_SECRET_KEY=CHANGE-ME-super-secret-key-2026-ai-store-builder
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Store Limits ──
MAX_STORES_FREE=3
MAX_STORES_PRO=20
MAX_STORES_ENTERPRISE=1000

# ── AI (leave empty for local fallback) ──
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# ── Email ──
EMAIL_PROVIDER=console     # console | resend | smtp
RESEND_API_KEY=
EMAIL_FROM_NAME=ويب فلو
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FRONTEND_URL=http://localhost:3000

# ── Supabase (optional — for real-time & conversation storage) ──
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=

# ── Payment Gateways (optional) ──
MOYASAR_API_KEY=
TAP_SECRET_KEY=

# ── Cloudflare R2 Storage (optional — uses local storage if empty) ──
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=store-images
R2_PUBLIC_URL=

# ── CORS ──
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","http://localhost:8000"]
